from setuptools import setup, find_packages

setup(
    name='snippets_catalog',
    version='1.0.0',
    url='',
    author='Carlos Matías de la Torre',
    author_email='cmdelatorre@gmail.com',
    description='Provee un listado de snippets para ser cargados en un Jupyter Notebook',
    packages=find_packages(),
    install_requires=[],
)
